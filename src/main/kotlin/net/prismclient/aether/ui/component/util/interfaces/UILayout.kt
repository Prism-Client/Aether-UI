package net.prismclient.aether.ui.component.util.interfaces

/**
 * [UILayout] is an interface to define a Layout component.
 *
 * @author sen
 * @since 1.0
 */
interface UILayout {
    /**
     * [updateLayout] is invoked when the layout is in need of an update.
     */
    fun updateLayout()
}